from __future__ import annotations

# Worker functions are embedded within controller.run_batch using ThreadPoolExecutor.
# This module is reserved for future expansion (e.g., process pools or separate worker classes).

